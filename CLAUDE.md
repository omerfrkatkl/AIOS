# CLAUDE.md — interim rules (restructure window, F0–F3)

The single authoritative map is `PLAN.md`. Read it at every session start.

1. Update the Progress table (§8) of `PLAN.md` at every session end.
2. No rule from the archived system applies; `arsiv/` is reference only.
3. Decision discipline still holds: evidence tags, T-A/B/C logic, append-only records.
4. Do not start new managed projects before the pilot phase (F8).
5. This file is temporary — replaced by the new rules file in F3.
